extends ../layouts/header.pug
block title
    title= title

block bodyContent 
    div(style={margin:"20px"})
        h4(style={color:'red'})= error.message
        h3 #{error.status} 
        p= error.stack

